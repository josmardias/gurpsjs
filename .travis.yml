sudo: false

branches:
  only:
    - master
    - devel

language: node_js

cache:
  directories:
    - node_modules

env:
  matrix:
    - COMMAND=browserstack
    - COMMAND=coverage

before_install:
- npm install codeclimate-test-reporter

script: bash travis.sh $COMMAND
